meta {
  name: 3. CatalogAPI
  seq: 3
}

docs {
  Antes desta pasta: Login admin (adminToken) e Login user (userToken, userId).
  Create game grava gameId; Create game to delete grava gameToDeleteId.
}
