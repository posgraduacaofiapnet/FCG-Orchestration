meta {
  name: Delete game (Admin)
  type: http
  seq: 6
}

delete {
  url: {{catalogUrl}}/api/games/{{gameToDeleteId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

assert {
  res.status: eq 204
}
