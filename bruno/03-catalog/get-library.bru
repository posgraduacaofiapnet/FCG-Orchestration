meta {
  name: Get library (User)
  type: http
  seq: 8
}

get {
  url: {{catalogUrl}}/api/library/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

assert {
  res.status: eq 200
}
